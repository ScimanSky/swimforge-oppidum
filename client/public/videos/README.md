Place optional landing tour video here:

- `swimforge-tour.mp4` (recommended)

The landing page (`client/src/pages/Home.tsx`) will try to autoplay this video
inside the Tour card (muted, loop, playsInline). If the file is missing or
fails to load, it automatically falls back to an animated image tour.

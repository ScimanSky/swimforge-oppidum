# Sample Data (Demo Mode)

This folder contains anonymous demo data that can be inserted into Supabase for a quick preview without provider credentials.

## Usage
1) Open Supabase SQL editor
2) Run `seed.sql`

This creates a demo user and two swim activities.

# Legacy MySQL Snapshots

These files are archived pre-PostgreSQL Drizzle snapshots kept for historical reference only.

They are intentionally excluded from `drizzle/meta/` because they are not compatible with the current PostgreSQL Drizzle toolchain.
